<div class="container px-1">
  <% content_for :title, "Birthdays" %>
  <h1 class='mb-1'>
  <%= link_to new_birthday_path, data: { turbo_frame: dom_id(Birthday.new)}, alt: 'New Birthday', class: 'btn add-button' do %>
                              <i class="bi bi-plus-lg me-md-1"></i><span class="d-none d-md-inline">Birthday 🎁 🎈</span><% end %> </h1>
  <%= turbo_frame_tag Birthday.new do %>
    <% render 'empty_table_message' if @birthdays.empty? %>
  <% end %>
  <%= turbo_frame_tag 'items-and-filter' do %>
    <%= render 'sort_selection' unless @birthdays.empty? %> 
    <%= turbo_frame_tag 'birthdays' do %>
      <%= render 'birthdays_table' unless @birthdays.empty? %>
    <% end %>
  <% end %>
</div>

